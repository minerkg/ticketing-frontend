import { TicketingDateTimePipe } from './ticketing-date-time-pipe';

describe('TicketingDateTimePipe', () => {
  it('create an instance', () => {
    const pipe = new TicketingDateTimePipe();
    expect(pipe).toBeTruthy();
  });
});
